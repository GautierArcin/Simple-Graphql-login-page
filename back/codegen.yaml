schema: "./src/type-defs.ts" # GraphQL types (input file)
generates:
  ./type/gql-types.d.ts: # Typescript types (output generated file)
    plugins: # List of needed plugins (installed as devDeps)
      - typescript
